import ProductDetailContainer from "../containers/ProductDetailContainer";

/**
 * Product Detail page entry point.
 * Renders the ProductDetailContainer component that handles fetching and displaying products.
 *
 * @returns {JSX.Element} - The rendered product detail component.
 */
function ProductDetail() {
  return <ProductDetailContainer></ProductDetailContainer>;
}

export default ProductDetail;
