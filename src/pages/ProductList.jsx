import ProductListContainer from "../containers/ProductListContainer";

/**
 * Product List page entry point.
 * Renders the ProductListContainer component that handles fetching and displaying products.
 *
 * @returns {JSX.Element} The rendered product list component.
 */
function ProductList() {
  return <ProductListContainer></ProductListContainer>;
}

export default ProductList;
